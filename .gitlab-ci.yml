---
pages:
  tags:
    - docker
  stage: deploy
  script:
    - "cd templates"
    - "find -print > ./tree"
    - "cd ../"
    - "mv templates public"
  artifacts:
    paths:
      - public
  only:
    - main
